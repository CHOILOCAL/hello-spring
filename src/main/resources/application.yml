# Log4j
#logging.config: classpath:logback-${spring.profiles.active}.xml

# port
server:
  port: 9000
  servlet:
    context-path:
    encoding:
      charset: UTF-8
      enabled: true


logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE

# MVC
#spring.mvc.view:
#  prefix: /WEB-INF/jsp/
#  suffix: .jsp

# google smtp server
spring:
#  profiles: local
  h2:
    console:
      enabled: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: hyunchoi@hist.co.kr
    password: sgglbprtchvgmuqm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/damdeeng?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1234
    devtools:
      livereload:
        enabled: true
      freemarker:
        cache: false
    mvc:
      hiddenmethod:
        filter:
          enabled: true
  resources:
    chain:
      strategy:
        content:
          enabled: true
          paths: /**
        fixed:
          paths: /js/lib/
          version: v12

---
spring:
  profiles: dev
  resources:

---
spring:
  profiles: test
  resources:

---
spring:
  profiles: prod
  resources:

# 관리자 이메일
system.info.email: choihyunji1103@gmail.com

# jwt token
props.jwt.expire-access: 86400000   # 1일
props.jwt.expire-refresh: 172800000 # 2일

# DB
#spring:
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/damdeeng?serverTimezone=UTC&characterEncoding=UTF-8
#    username: root
#    password: 1234
#  devtools:
#    livereload:
#      enabled: true
#    freemarker:
#      cache: false
#  mvc:
#    hiddenmethod:
#      filter:
#        enabled: true

# JPA
spring.jpa:
  database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  open-in-view: false
  show-sql: true
  hibernate:
    format_sql: true
    ddl-auto: create

#spring.jpa:
#  database-platform: org.hibernate.dialect.PostgreSQLDialect
#  show-sql: true
#  hibernate.ddl-auto: none # spring.datasource.initialization-mode=always 설정인 경우, 초기 데이터에서 제약조건 위반이 발생할 수 있으므로 ddl-auto = 'create' 또는 'create-drop' 설정
#  properties.hibernate.temp.use_jdbc_metadata_defaults: false

